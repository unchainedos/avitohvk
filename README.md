# avitohvk

Бартерная площадка, где пользователи выставляют вещи, отмечают, что хотят получить взамен (список желаемого), а система сводит эти желания в сделки с последующим обменом.

Всего есть два способа обмена:

- **цепочка сделок** — многосторонний согласуемый обмен. Участники по очереди присоединяются к цепочке посредством операции **chown**, каждый указывает, что готов отдать; цепочка становится настоящей сделкой только тогда, когда кто-то замыкает круг вещью, которую хочет самый первый участник. Дальше сделку нужно подтвердить не всем сразу, а по цепочке — от последнего получателя назад к первому: каждый может подтвердить свою часть только после того, как тот, кому он передаёт вещь, уже подтвердил свою. Сорванная на любом этапе сделка отменяется целиком, а не зависает в промежуточном состоянии.
- **точечная передача** — быстрый односторонний ход, то есть прямой обмен между двумя пользователями (частный случай цепочки сделок).

Обе операции конкурируют за одни и те же вещи, поэтому в основе лежит набор гарантий на уровне базы данных: вещь не может «утечь» из уже подтверждённой сделки через точечную передачу, а параллельные попытки застолбить одну и ту же вещь корректно разрешаются в пользу только одного участника.

---

## Инструкция по запуску

Нужно: Go 1.25, PostgreSQL 17, Docker и Docker Compose .

### Docker Compose

1. Создать `.env` в корне репозитория. Эти значения использует только сам Docker Compose, чтобы поднять контейнер с Postgres — само приложение адрес базы и учётные данные берёт из `config/config.yaml`, где они уже прописаны такими же (`avitohvk`/`avitohvk`/`avitohvk`, хост `postgres`). Если менять `.env`, эти же значения нужно вручную поменять и в `config.yaml`, иначе приложение не подключится к базе:

   ```env
   POSTGRES_USER=avitohvk
   POSTGRES_PASSWORD=avitohvk
   POSTGRES_DB=avitohvk
   POSTGRES_PORT=5432
   PGHOST=postgres
   PGPORT=5432
   ```

2. Поднять контейнер:

   ```bash
   docker compose up --build
   ```

   Поднимаются три сервиса: сам Postgres, разовый контейнер, который дожидается его готовности и по порядку применяет все миграции из `migrations/`, и само приложение — оно начинает слушать `:8080` уже после того, как база готова и смигрирована.

### Фронтенд

```bash
cd web
npm install
npm run dev
```

### Тесты и проверки

```bash
go build ./...
go vet ./...
golangci-lint run ./...
go test ./...
go test -race ./...
```

- модульные тесты и интеграционные тесты хранилища не требуют отдельной настройки — им нужен только работающий Docker, Postgres для них поднимается автоматически.

---

## Технологии

**Backend**
- Go 1.25
- [chi](https://github.com/go-chi/chi) — HTTP-роутинг
- [pgx/v5](https://github.com/jackc/pgx) + `pgxpool` — драйвер и пул соединений PostgreSQL
- PostgreSQL 17
- [golang-jwt/v5](https://github.com/golang-jwt/jwt) — JWT (HS256), передаётся через httpOnly-cookie
- [viper](https://github.com/spf13/viper) — конфигурация из YAML
- [testcontainers-go](https://golang.testcontainers.org/) — интеграционные тесты против настоящего Postgres в одноразовом контейнере
- [golangci-lint](https://golangci-lint.run/) — статический анализ (см. `.golangci.yml`)
- Docker / Docker Compose — контейнеризация и локальный запуск стека

**Frontend** (`web/`)
- React 19 + TypeScript + Vite
- MobX (стейт-менеджмент), React Router, React Hook Form, Zod, TailwindCSS

---

## Структура проекта

```
avitohvk/
├── cmd/main.go              — точка входа: сборка зависимостей и подключение маршрутов
├── config/                  — загрузка конфигурации приложения
├── migrations/               — SQL-миграции по номерам, у каждой есть пара для отката
├── scripts/                  — вспомогательные скрипты, включая сценарную проверку
├── web/                       — веб-интерфейс (React, TypeScript, Vite)
└── internal/
    ├── domain/                 — модели предметной области: вещь, пользователь, список
    │                             желаемого, сделка, заявка на участие, точечная передача
    ├── dto/                     — структуры запросов и ответов HTTP-слоя, отдельные от domain
    ├── errors/                  — сквозные ошибки уровня приложения, из которых собираются
    │                             ответы с нужным статус-кодом
    ├── server/                  — запуск и остановка HTTP-сервера, включая штатное
    │                             завершение работы по сигналу или по таймауту
    ├── repository/              — слой хранения, отдельный пакет на каждую сущность:
    │   ├── chown/                  — точечная передача прав на вещь
    │   ├── deal/                    — создание сделки, чтение и обновление её статуса
    │   │                             и срока действия, блокировка записи сделки на время
    │   │                             операций над ней, чтобы они не пересекались друг с другом
    │   ├── proposal/                — заявки участников на вход в сделку, порядок их
    │   │                             подтверждения, разрешение конфликтов между сделками
    │   │                             за одну и ту же вещь
    │   ├── item/                    — вещи пользователей
    │   ├── user/                    — учётные записи
    │   ├── wish/                    — список желаемого
    │   ├── search/                  — поиск по вещам
    │   └── dbtest/                  — общая инфраструктура для тестов, которым нужна
    │                                 настоящая база: поднимает Postgres в одноразовом
    │                                 контейнере и выдаёт каждому тесту свою чистую,
    │                                 уже смигрированную базу
    ├── service/                 — бизнес-логика поверх хранилища, разбита на пакеты
    │                             так же, как repository
    └── transport/
        ├── router/                — сборка маршрутов из всех обработчиков в одно приложение
        ├── middleware/             — проверка токена авторизации на входящих запросах
        ├── utilhttp/               — общие для всех обработчиков функции чтения и записи
        │                             JSON и перевод внутренних ошибок в HTTP-ответ
        └── handler/                — обработчики запросов, по пакету на ресурс:
            ├── auth/                  — вход и регистрация
            ├── user/, users/           — профиль пользователя и просмотр чужого профиля
            ├── item/                   — вещи
            ├── wish/                   — список желаемого
            ├── search/                 — поиск
            ├── chown/                  — точечная передача прав
            ├── deal/                   — просмотр сделки
            └── props/                  — вход в сделку и согласование заявок
```

---

## Особенности реализации

- **Конкурентный доступ к вещам.** Несколько желающих могут одновременно претендовать на одну вещь — решает это построчная блокировка нужной записи внутри транзакции: параллельный запрос к той же строке ждёт своей очереди, а не читает устаревшие данные.
- **Приоритет по списку желаемого.** Если вещь хотят несколько человек, получает её тот, с кем ранее всего согласовно предложение.
- **Формирование цепочки сделок.** Цепочка становится настоящей сделкой только тогда, когда кто-то предлагает именно ту вещь, которую хочет первый участник — с этого момента у сделки появляется срок на подтверждение, и присоединить нового участника уже нельзя.
- **Порядок подтверждения.** Участники подтверждают сделку не одновременно, а по цепочке от последнего получателя назад к первому — каждый может подтвердить свою часть только после того, как тот, кому он передаёт вещь, уже подтвердил свою. Исключение — тот, кто создал сделку, и тот, кто предложил замыкающую вещь: они подтверждают в любой момент.
- **Блокировка вещи в момент подтверждения именно её заявки**, а не только при закрытии всей цепочки — иначе участник, освобождённый от порядка подтверждения, успел бы увести обещанную вещь через точечную передачу ещё до того, как цепочка соберётся целиком.
- **Каскадная отмена.** Любой путь отмены сделки — по инициативе участника, по истечении срока или потому что другая сделка успела первой забрать себе ту же вещь — одним каскадом переводит все ещё не подтверждённые заявки в отменённые и снимает блокировку ровно с тех вещей, которые держит именно эта сделка, не задевая чужие. На уровне схемы данных заявки участников также удаляются каскадно вместе со сделкой, если сама сделка удаляется.
- **Обработка блокирующих коллизий.** Отмена конкурирующей сделки иногда требует вмешаться в строки другой, параллельно закрываемой сделки — если два таких изменения сталкиваются одновременно и база обнаруживает взаимную блокировку, проигравшая сторона автоматически и безопасно повторяет попытку.
- **Защита от гонок внутри одной сделки.** Подтверждение, отзыв заявки и присоединение нового участника к одной и той же сделке не могут выполниться одновременно — иначе, например, отзыв и подтверждение могли бы разойтись и оставить подтверждённую заявку висящей на уже отменённой сделке.
- **Пересечение точечной передачи и сделки.** Если вещь уже заблокирована подтверждённой сделкой, точечная передача её больше не тронет; более лёгкая, ничем не подтверждённая пометка от точечной передачи, наоборот, не мешает присоединению вещи к сделке и снимается автоматически.
- **Авторизация.** JWT передаётся не в заголовке запроса, а в httpOnly-cookie — вход выставляет её сам, дальнейшие запросы браузер прикладывает автоматически.

---

## Распределение ответственности

### Реализация схемы данных и миграций , логики связывания пользователей в цепочки и этапов согласования сделки, написание инфраструктуры сервера  (Гольчанский Максим)

- **Схема хранения обмена** — спроектированы и написаны миграции для сделок, заявок участников и их состояний, включая ограничения целостности и автоматическое обновление отметки времени изменения.
- **Точечная передача** — реализована операция прямого обмена между пользователями
- **Формирование и согласование цепочки сделок** — реализовано присоединение участников к сделке, определение получателя по списку желаемого, порядок подтверждения по цепочке, срок действия сделки и его продление при подтверждении.
- **Каскадная отмена сделки** — реализовано, что любой путь отмены (по инициативе участника, по истечении срока, из-за конфликта с другой сделкой) одним каскадом переводит все ещё не подтверждённые заявки в отменённые и снимает блокировку ровно с тех вещей, которые держит именно эта сделка.
- **Разрешение конфликтов за одну и ту же вещь** — реализована логика, которая при подтверждении сделки автоматически отменяет другие сделки, претендующие на ту же вещь, и корректно освобождает то, что они успели заблокировать.
- **Обработка блокирующих коллизий и защита от гонок** — устойчивость к взаимным блокировкам базы данных при параллельной отмене конкурирующих сделок (автоматический безопасный повтор операции), а также защита одной и той же сделки от одновременных операций над ней, чтобы её отмена и подтверждение не могли разойтись друг с другом.
- **HTTP-слой сделок и точечной передачи** — обработчики запросов, маршрутизация, промежуточный слой проверки авторизации, общие вспомогательные функции для чтения и записи JSON.
- **Инфраструктура сервера** — запуск, остановка и штатное завершение работы сервера.
- **Тестовое покрытие** — табличные модульные тесты на подставных зависимостях, интеграционные тесты на настоящей базе данных (включая написание общей инфраструктуры для них).
